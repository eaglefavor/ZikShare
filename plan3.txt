So we need to introduce Digital Items.
The user provided a PDF Marketplace spec:
Database Schema: `digital_products`, `orders` tables.
Upload flow, Payment Flow, Webhook flow.

The user says: "This is a little document I stumbled upon, I like the idea and I want to ADPAT A SIMILAR OPTIONAL FEATURE INTO ZIKSHARE. This is not a photocopy but an adaptation to fit the current system we have. How we want to implement ours is: The sub account (seller) takes 100% Set percentage_charge: 0. Then we want to make sure the buyer bears pay stack percentage fees."

Ok, I'll adapt the marketplace into Zikshare.

Here's the plan:
1. **Database schema**: Since Zikshare already has a `listings` table, we can either:
  a. Add digital product fields to `listings` (like `is_digital`, `file_path`, etc.).
  b. Or use the provided `digital_products` and `orders` tables. I will create `digital_products` and `orders` as a separate table as the document suggests, but integrate it seamlessly into the UI (maybe a "Digital Materials" section or category). Actually, if we add `type: 'digital' | 'physical'` to listings, it might be simpler to integrate with the feed. However, sticking closer to the provided document (with `digital_products` table) ensures we follow the specification. Let's create an SQL file for the user to run. Since I can't run it myself (I don't have access to the production Supabase database credentials, just a generic prompt environment), I'll make a `supabase/migrations/01_digital_marketplace.sql` file.

2. **Frontend UI adaptations**:
  a. `App.jsx`: Add routes for Digital Post page, Digital Checkout/Success.
  b. `HomePage.jsx` / `SearchPage.jsx`: Either display digital products alongside listings, or add a toggle/tab for Digital Products. I will add a category "Digital" to the normal `listings` UI if possible, OR create a separate section. Let's look at `PostPage.jsx`. Categories are: `['Electronics', 'Fashion', 'Books', 'Hostel', 'Services']`. Let's add `'Digital PDF'` as a category. If category is `'Digital PDF'`, we prompt for a PDF file instead of photos. Wait, this means we should combine them into `listings`. The provided schema has `digital_products`. I will adapt it so we use the user's provided `digital_products` table to keep the "original storage path" and other metadata.

  Let's refine:
  - Add `src/pages/PostDigitalPage.jsx` for uploading PDFs.
  - Update `BottomNav` or `ProfilePage` or `HomePage` to link to it. Maybe in `PostPage.jsx`, add a tab at the top: "Physical Item" | "Digital PDF".
  - Add `ItemDetailPage.jsx` update: if it detects a digital product, show the Paystack Checkout button and digital info.
  - Paystack logic: Calculate the fee so the buyer bears it, and seller gets 100%. The user gave live keys, I will put them in `.env`.
  - Add `PaymentSuccess.jsx`.
  - Add `src/components/PaystackCheckout.jsx`.

3. **Supabase Edge Function / Python Backend**:
  The spec talks about a Python service on Render.com to encrypt the PDF. I should probably create the supabase edge function code in `supabase/functions/paystack-webhook/index.ts` so the user has the code. For the PDF service, I can create `pdf-service/main.py`. The user just wants me to adapt the code into the ZikShare codebase.

Let's do this step-by-step.
First, check the categories in `PostPage.jsx`.
