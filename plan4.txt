Wait, the user wants me to implement this. To make sure it fits ZikShare, the easiest way to integrate "Digital Products" into the feed is to add them to `listings` with a `type="digital"` or `category="Digital PDF"`, and add the new fields (`fileUrl`, `fileSize`, `pages`, etc.) as optional fields in `listings`. But wait, the user specifically provided a SQL schema for `digital_products` and `orders`, and they said "This is a little document I stumbled upon, I like the idea and I want to ADPAT A SIMILAR OPTIONAL FEATURE INTO ZIKSHARE. This is not a photocopy but an adaptation to fit the current system we have."

"adaptation to fit the current system we have" means we should blend it with our `listings` table, OR keep them separate but display them in the UI. In `HomePage.jsx`, the feed fetches from `listings`. Let's add a "Digital Market" tab or update `HomePage.jsx` to fetch both.

Let's modify `src/lib/database.js` to create functions for digital products:
`createDigitalProduct`, `getDigitalProducts`, `createOrder`, `updateOrder`.

Wait, actually, I should just modify `PostPage` to have two tabs: "Physical Item" and "Digital PDF".
If "Digital PDF", they upload a PDF file instead of photos. It creates a record in `digital_products`.
Then in `HomePage`, we show a tab bar at the top: "Physical" | "Digital".
Or just integrate them together in the `listings` table, by renaming `digital_products` to `listings`? No, the user provided a SQL schema with `digital_products`. I'll stick to creating `digital_products`.

Wait! The user says:
"How we want to implement ours is: The sub account (seller) takes 100% Set percentage_charge: 0. Then we want to make sure the buyer bears pay stack percentage fees."
This means the focus of the adaptation is the **Paystack Fee Logic**.

Paystack Fee Calculation:
Total = Amount + Paystack Fee
Paystack Fee = 1.5% of Total + 100 NGN (if >= 2500). Max fee is 2000 NGN.

Formula to pass the fee to the buyer:
Total Charge = (Product Price + 100) / (1 - 0.015)

Let's write a `calculatePaystackFee` function in `src/lib/paystack.js`.

Let's do the following plan:
1. Review Plan
2. Add Paystack component and config.
3. Add Digital Product Upload to PostPage.
4. Add Digital Product Details to ItemDetailPage.
5. Setup the Supabase SQL migration for the user.
