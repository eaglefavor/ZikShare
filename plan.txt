To add Paystack integration for a digital marketplace as described:

1. Look into the current items structure (physical items logic vs digital items logic).
2. The user has given a complete SQL setup for Supabase and frontend code snippets for React/Vite.
3. We need to implement the Digital Products upload and checkout flow with Paystack. Wait, this task is an "adaptation" as the user states:
"This is a little document I stumbled upon, I like the idea and I want to ADPAT A SIMILAR OPTIONAL FEATURE INTO ZIKSHARE. This is not a photocopy but an adaptation to fit the current system we have. How we want to implement ours is: The sub account (seller) takes 100% Set percentage_charge: 0. Then we want to make sure the buyer bears pay stack percentage fees."

4. What we need to do is adapt the user's provided feature into the codebase. Let's list what we actually need to modify or create based on the user's specific request:
- Adapt the provided SQL schema, functions, and policies for Digital Products and Orders in Supabase.
- Modify the database schema to add the columns to users (`paystack_subaccount_code`, etc.).
- The user wants the buyer to bear the Paystack percentage fees, so we need to calculate the fee and add it to the total amount.
- Ensure the seller takes 100% by setting `percentage_charge: 0` (or its equivalent in Paystack splits). Wait, Paystack's `split_code` allows this.
- Build the Paystack integration frontend part (checkout) using `react-paystack`.
- Build the Webhook handler (edge function in Supabase) to handle Paystack success events.
- Adapt the product view (`ItemDetailPage`) to distinguish between physical and digital products, or create a separate `DigitalProductDetail` page.
- Add an upload digital product page.

Actually, the user only specified the frontend codebase (`src` is available), and no supabase functions are present yet.

Wait, the prompt says: "I want to ADPAT A SIMILAR OPTIONAL FEATURE INTO ZIKSHARE. This is not a photocopy but an adaptation to fit the current system we have. How we want to implement ours is: The sub account (seller) takes 100% Set percentage_charge: 0. Then we want to make sure the buyer bears pay stack percentage fees."

Does the user want us to create the components, or specifically write the SQL / Edge Function / React code that matches their "100% to seller, buyer bears fee" instruction?

The user provided the text of the PDF Marketplace tutorial and then added:
"This is a little document I stumbled upon, I like the idea and I want to ADPAT A SIMILAR OPTIONAL FEATURE INTO ZIKSHARE. This is not a photocopy but an adaptation to fit the current system we have. How we want to implement ours is: The sub account (seller) takes 100% Set percentage_charge: 0. Then we want to make sure the buyer bears pay stack percentage fees."

We need to implement this new "Digital PDF" feature in the current ZikShare app (which we found in `src/`).

Let's check `package.json`. It doesn't have `react-paystack`.
